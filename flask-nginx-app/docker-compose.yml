services:
  app:
    image: YOUR_DOCKERHUB_USERNAME/flask-app:latest
    ports:
      - "5000:5000"

  nginx:
    image: YOUR_DOCKERHUB_USERNAME/nginx-proxy:latest
    ports:
      - "80:80"
    depends_on:
      - app